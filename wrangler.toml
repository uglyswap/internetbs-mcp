name = "internetbs-mcp"
main = "src/worker.ts"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

# No Internet.bs credentials needed here!
# Each user passes their own credentials via HTTP headers.
#
# Optional: lock down the endpoint with a server-level token
#   wrangler secret put MCP_AUTH_TOKEN
